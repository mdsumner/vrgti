% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_vrt.R
\name{validate_vrt}
\alias{validate_vrt}
\title{Validate that a VRT represents a simple mosaic}
\usage{
validate_vrt(vrt_info)
}
\arguments{
\item{vrt_info}{A list as returned by \code{\link[=parse_vrt]{parse_vrt()}}.}
}
\value{
Invisibly, \code{TRUE} if valid. Throws an error with a diagnostic
message if validation fails.
}
\description{
Checks whether a VRT file is suitable for conversion to a GTI tile index.
A "simple" VRT mosaic must satisfy:
}
\details{
\itemize{
\item All sources are \code{SimpleSource} or \code{ComplexSource} (no \code{AveragedSource},
\code{KernelFilteredSource}, etc.)
\item No \code{subClass} on bands (no \code{VRTWarpedDataset}, \code{VRTDerivedRasterBand})
\item All bands reference the same set of source files with the same DstRect
layout (standard multi-band mosaic)
\item SrcRect and DstRect dimensions match (no within-VRT resampling)
}
}
\examples{
\dontrun{
info <- parse_vrt("mosaic.vrt")
validate_vrt(info)
}
}
